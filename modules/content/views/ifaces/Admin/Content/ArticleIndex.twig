{% extends layout %}

{% block page_content %}

    <ul>
        {% for article in articles %}
            <li>
                <a href="{{ article.url }}">{{ article.label }}</a>
            </li>
        {% endfor %}
    </ul>

{% endblock %}
