{% extends layout %}

{% block page_content %}

    <ul>
        {% for post in posts %}
            <li>
                <a href="{{ post.url }}">{{ post.label }}</a>
            </li>
        {% endfor %}
    </ul>

{% endblock %}
