{% extends "@templates/shortcode-editor/attribute-field" %}

{% block attribute_control %}
  <div class="row">
    <div class="input-field col s12">
      <input value="{{ value }}" id="{{ id }}" name="{{ name }}" type="text" class="validate"
             placeholder="{{ default }}"/>
      <label for="{{ id }}">{{ label }}</label>
    </div>
  </div>
{% endblock %}
