{% for image in images %}
    <img src="{{ image.src }}" alt="{{ image.alt }}" title="{{ image.title }}" srcset="{{ image.srcset }}" class="img-responsive" />
{% else %}
    no images
{% endfor %}

{# TODO type-related blocks #}
